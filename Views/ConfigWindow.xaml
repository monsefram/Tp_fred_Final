<Window x:Class="Tp_Final_Fred.Views.ConfigWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Configuration" Width="400" Height="250">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel>

            <TextBlock Text="Langue :" FontWeight="Bold" />
            <ComboBox SelectedItem="{Binding Language}"
                      ItemsSource="{Binding Languages}"
                      Width="150"/>

            <TextBlock Text="Token API :" Margin="0,20,0,0" FontWeight="Bold"/>
            <TextBox Text="{Binding ApiKey}" Width="250"/>
        </StackPanel>

        <Button Grid.Row="2"
                Content="Enregistrer"
                HorizontalAlignment="Left"
                Width="98"
                Margin="262,30,0,30"
                Command="{Binding SaveCommand}" />

    </Grid>
</Window>
